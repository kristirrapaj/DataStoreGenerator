<Window SizeChanged="OnwindowSizeChanged" x:Class="DS_Generator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="1000">
    <Grid Background="#F0F0F0">
        <!-- Config Path -->
        <StackPanel x:Name="ConfigPanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0, 5, 0, 0">
            <Label Content="Select configuration file path:"></Label>
            <!-- Browse Button -->
            <Button Content="Browse" Click="OnBrowseButtonClick"></Button>
        </StackPanel>
        <StackPanel x:Name="OutputPanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0, 30, 0, 0">
            <Label Content="Select final write path:"></Label>
            <!-- Browse Button -->
            <Button Content="Browse" Click="OnFinalBrowseButtonClick"></Button>
        </StackPanel>
        <!-- Database Type -->
        <StackPanel x:Name="DatabasePanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,60,0,0">
            <Label Content="Select Database Type: " Foreground="#333" />
            <ComboBox x:Name="CbDataProviderType" Width="200" Height="20"
                      ItemsSource="{Binding AvailableDataProviders}"
                      SelectionChanged="OnDataProviderSelectionChanged"
                      HorizontalAlignment="Stretch" />
        </StackPanel>
        
        

        <!-- DataProvider Type -->
        <StackPanel x:Name="ProviderPanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,90,0,0">
            <Label Content="Select DataProvider Type: " Foreground="#333" />
            <ComboBox x:Name="CbDatabaseType" Width="200" Height="20"
                      ItemsSource="{Binding AvailableDatabases}"
                      SelectionChanged="OnDatabaseSelectionChanged"
                      HorizontalAlignment="Stretch" />
        </StackPanel>

        <!-- Select Table -->
        <Grid x:Name="MyGrid">
            <StackPanel x:Name="TablesPanel" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,120,0,0">
                <Label Content="Select Table: " Foreground="#333" />
                <ListView x:Name="CbDataTableType" Width="150" Height="100"
                          ItemsSource="{Binding AvailableDataTables}"
                          SelectionChanged="OnDataTableSelectionChanged"
                          HorizontalAlignment="Stretch" />
            </StackPanel>
            <StackPanel x:Name="SelectedTablesPanel" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,120,0,0">
                <Label Content="Tables selected: "></Label>
                <ListBox Name="MyListBox" Width="200" Height="90" ItemsSource="{Binding _tablesSelected}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                                <TextBlock Text="{Binding}"/>
                                <Button Name="remove_Button" Content="REMOVE"  Click="OnTableRemoveButtonSelect"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Grid>
        
        
        <StackPanel x:Name="GeneratePanel" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,250,0,0">
            <Button Content="Generate" Click="OnGenerateButtonSelected"></Button>
        </StackPanel>
        <Label x:Name="ConsoleText" HorizontalContentAlignment="Center" HorizontalAlignment="Center" FontSize="12" Margin="0, 300, 0, 0"></Label>
    </Grid>
</Window>